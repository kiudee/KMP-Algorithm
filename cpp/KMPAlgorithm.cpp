/*
 * KMPAlgorithm.cpp
 *
 *  Created on: 27.01.2011
 *      Author: Karlson
 */
#include <iostream>
#include <string>
#include <vector>
using namespace std;

string run(string &t1, string &t2) {
    vector<char> minString, maxString;
    if (t1->length() < t2->length()) {
        minString = t1->;
    } else {
    }

    int minLength, maxLength;
    return NULL;
}

vector<char> toCharVector(string &s) {
    vector<char> result(s->length());
    for (int i = 0; i < s->length(); i++) {
        result[i] = (*s)[i];
    }

    return result;
}

int main() {
    return 0;
}

